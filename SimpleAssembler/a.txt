start:
add t0, zero, zero

beq t0, zero, forward_branch

addi t0, t0, 1

forward_branch:
jal t1, backward_jump

addi t2, t2, 5

backward_jump:
beq t1, zero, end

jal t3, start

end:
slt t4, t0, t2

jal t0, 1048572

sw t0, 40(t1)